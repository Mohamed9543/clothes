{
  "name": "libas",
  "private": true,
  "version": "0.1.0",
  "description": "Libas - e-commerce vetements avec avatar 3D et chatbot IA",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=20"
  }
}
