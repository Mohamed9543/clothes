# Backend API base URL. On a physical device or Android emulator, replace
# localhost with your machine's LAN IP (e.g. http://192.168.1.10:3000) since
# "localhost" resolves to the device itself, not your dev machine.
EXPO_PUBLIC_API_URL=http://localhost:3000
