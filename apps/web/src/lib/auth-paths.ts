// Pages reachable without being logged in — the login/register/password-reset flow.
// Shared by AuthGate (redirect) and the layout chrome (Navbar/Footer trim).
export const AUTH_PATHS = ['/login', '/register', '/forgot-password', '/verify-code', '/reset-password'];
